{
  "_comment": "Optional secret overlay for mcp.json servers. Copy to mcp-secrets.json and fill in real values. Keep mcp-secrets.json out of git (it is gitignored). The installer does NOT auto-create this file, so unconfigured servers are not started with bogus credentials.",
  "neon": {
    "headers": {
      "Authorization": "Bearer <YOUR_NEON_API_KEY>"
    }
  },
  "exa": {
    "env": {
      "EXA_API_KEY": "<YOUR_EXA_API_KEY>"
    }
  }
}
