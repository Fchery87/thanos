import { describe, expect, it } from "vitest";

describe("prompt-system test discovery", () => {
  it("executes the dedicated prompt test suite", () => {
    expect("prompt-system").toBe("prompt-system");
  });
});
